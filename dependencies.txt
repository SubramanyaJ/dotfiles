# Do NOT run this as a script.
# Copy and paste to suit your needs.

# All dependencies
git pavucontrol zoxide fzf pulseaudio libevdev pkg-config powertop tlp fonts-knda node nodejs npm zip unzip gunzip git-delta delta network-manager ssh libreoffice i3-wm vim doxygen dot graphviz tree bat lshw nmap gimp texlive texlive-base texlive-bibtex-extra gdb clang gcc flameshot kitty strace pcmanfm curl file wget make cmake ninja-build build-essential rofi texlive-fonts-recommended texlive-latex-recommended texlive-lang-english w3m aria2 unrar-free fonts-firacode fd-find i3lock nasm xclip chromium ffmpeg pdftk vainfo zathura zsh redshift

# Install gf
cd ~
git clone https://github.com/nakst/gf.git
cd gf
./build.sh
cd ~

# Oh-My-Zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

# Install evremap on Debian (God forbid)
cd ~
git clone https://github.com/wez/evremap.git
cd evremap
sudo apt install libevdev-dev pkg-config
cargo build --release
cd ~

# Install neovim on Debian
cd ~
git clone https://github.com/neovim/neovim.git
cd neovim
make CMAKE_BUILD_TYPE=RelWithDebInfo
sudo make install
cd ~


